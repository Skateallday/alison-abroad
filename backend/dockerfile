FROM node:16-alpine

WORKDIR /app

# Install deps first for better caching
COPY package*.json ./
RUN npm install

# Copy the rest
COPY . .

# If your backend listens on another port, change this
EXPOSE 5000

# nodemon for dev
CMD ["npx", "nodemon", "server.js"]
